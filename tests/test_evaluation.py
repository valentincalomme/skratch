# import numpy as np

# import sklearn.metrics as sc  # for scikit-learn
# import utils.evaluation as sk  # for skratch


# evaluation_pairs = [
#     dict(sc=sc.accuracy_score, sk=accuracy),
#     dict(sc=sc.mean_squared_error, sk=mse),
#     dict(sc=sc.median_absolute_error, sk=mae),
#     dict(sc=sc.brier_score_loss, sk=sk.brier_score),
    
# ]
